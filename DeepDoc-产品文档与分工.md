# DeepDoc 产品文档与分工方案

> 面向长文档的可视化层级 RAG 系统
> 课程：智能计算系统设计 | 团队规模：3 人

---

## 一、项目概述

### 1.1 项目定位

**DeepDoc** 是一个面向百页级长文档的智能问答系统，核心特色是**可视化的层级化 RAG 检索**——让用户不仅能得到答案，还能"看见"AI 是如何在文档中思考和检索的。

### 1.2 解决的痛点

- 传统 RAG 在长文档场景下信息丢失严重，全局结构感知弱
- RAG 系统是黑盒，用户不知道答案从哪里来、是否可信
- 朴素分块策略破坏文档语义结构
- 缺乏对检索质量的可视化评估手段

### 1.3 核心价值主张

1. **看得见的检索**：可视化 AI 的思考与检索路径
2. **信得过的答案**：精确到段落的引用溯源 + 可信度评分
3. **撑得起的长文**：层级化索引应对百页级文档
4. **比得出的效果**：内置 baseline 对比模式，效果差异一目了然

### 1.4 目标用户场景

- 学生阅读论文、教材
- 研究人员综述多份文献
- 法律/合同文档分析
- 技术手册查询

---

## 二、系统架构

### 2.1 整体分层

```
┌─────────────────────────────────────────┐
│  展示层（前端可视化 + 交互）              │
├─────────────────────────────────────────┤
│  应用层（API 网关 + 业务编排）            │
├─────────────────────────────────────────┤
│  生成层（LLM 调用 + 引用标注）           │
├─────────────────────────────────────────┤
│  检索层（混合检索 + Rerank + 改写）       │
├─────────────────────────────────────────┤
│  索引层（层级索引构建 + 向量库 + BM25）   │
├─────────────────────────────────────────┤
│  解析层（多格式文档解析 + 结构化）        │
└─────────────────────────────────────────┘
```

### 2.2 技术栈

| 层级 | 技术选型 |
|------|---------|
| 前端 | React + TypeScript + TailwindCSS + shadcn/ui + ECharts + React Flow |
| 后端 | Python FastAPI + SSE |
| 向量库 | Qdrant |
| Embedding | bge-large-zh-v1.5 |
| Rerank | bge-reranker-v2-m3 |
| LLM | DeepSeek API / Claude API |
| 文档解析 | PyMuPDF + unstructured |
| 部署 | Docker Compose |

---

## 三、需求清单（按优先级）

### 🔴 P0 - 核心必做（系统能跑起来的最小闭环）

#### P0-1 文档解析与结构化
- 支持 PDF、Markdown、TXT 三种格式上传
- 提取文档层级结构（标题、章节、段落）
- 输出统一的结构化文档对象（含层级树）
- 处理表格、列表等基础结构

#### P0-2 多粒度切分与索引
- 实现段落级、句子级两种粒度的 chunking
- 章节级摘要生成（调用 LLM）
- 三级 embedding 入库（章节摘要 / 段落 / 句子）
- BM25 索引并行构建

#### P0-3 基础检索引擎
- 向量检索（dense）
- BM25 检索（sparse）
- RRF 融合算法
- Rerank 精排

#### P0-4 两阶段层级检索
- 第一阶段：在章节摘要层定位相关章节
- 第二阶段：在命中章节内的段落层精排
- 返回带层级路径信息的检索结果

#### P0-5 答案生成与引用
- 调用 LLM 基于检索结果生成答案
- 答案中标注引用编号（[1][2][3]）
- 引用对应到具体段落 ID 与位置

#### P0-6 后端 API 接口
- 文档上传接口
- 文档列表/删除接口
- 问答接口（同步版本）
- 引用详情查询接口

#### P0-7 前端基础交互
- 文档上传页
- 文档列表页
- 问答主界面（输入框 + 答案展示）
- 引用点击跳转 + 原文高亮

#### P0-8 baseline 实现（用于对比）
- 朴素 RAG 实现（固定大小 chunk + 单路向量检索）
- 与层级 RAG 共用同一套接口，便于对比

---

### 🟡 P1 - 重点拓展（决定展示效果的核心）

#### P1-1 流式"思考过程"展示 ⭐
- 后端用 SSE 流式输出每个阶段的状态
- 前端实时显示：分析问题 → 查询改写 → 检索摘要 → 下钻段落 → 生成答案
- 每阶段带耗时统计

#### P1-2 文档结构思维导图 ⭐
- 文档解析后渲染成可交互的层级树（思维导图）
- 节点可点击展开/折叠
- 检索时高亮命中的章节节点

#### P1-3 检索路径可视化 ⭐
- 用 React Flow 渲染检索路径图
- 节点：查询 → 摘要节点 → 段落节点 → 答案
- 边带相似度分数
- 支持动画播放检索过程

#### P1-4 文档热力图 ⭐
- 文档微缩视图（类似 VSCode 右侧缩略图）
- 根据检索相关度对段落染色
- 支持点击跳转

#### P1-5 对比演示模式 ⭐
- 同一问题，左右分屏展示朴素 RAG vs 层级 RAG
- 同步显示各自的检索结果与答案
- 底部展示指标对比（准确率、延迟、token）

#### P1-6 查询改写
- HyDE（生成假设性答案后再检索）
- 多查询扩展（一个问题改写成 3 个）
- 可在前端开关，对比效果

#### P1-7 性能监控面板
- 实时显示检索延迟分解（解析、检索、rerank、生成）
- Token 消耗统计
- 命中层级分布图

---

### 🟢 P2 - 加分功能（时间允许就做）

#### P2-1 多文档对比问答
- 支持选择多份文档同时提问
- 分栏展示每份文档的证据
- 答案按来源用不同颜色标注

#### P2-2 推荐问题与文档导读
- 上传后自动生成 5-10 个推荐问题
- 自动生成文档导读摘要
- 每章节生成关键词云

#### P2-3 引用可信度评分
- 每条引用计算可信度分数
- 综合考虑：检索分数、rerank 分数、答案-原文一致性
- 低可信度引用用警告色标记

#### P2-4 多轮对话
- 支持上下文相关的连续提问
- 历史对话管理

#### P2-5 实验评估系统
- 内置评估数据集
- 一键跑评估并出报告（准确率、召回率、延迟等）
- 消融实验配置化

---

### ⚪ P3 - 锦上添花（看时间）

- DOCX 格式支持
- 文档版本管理
- 用户系统与权限
- 答案导出（Markdown / PDF）
- 移动端适配

---

## 四、团队分工方案

### 4.1 分工原则

- **三人均可全栈**，按"垂直切片"划分而非"前端/后端"
- 每人负责一条**独立的技术线**，对外暴露清晰接口
- 通过**接口契约先行**让三人完全并行，互不阻塞
- 后期联调与集成由三人共同负责

### 4.2 角色划分

#### 👤 成员 A：文档处理 & 索引构建线（数据侧）

**P0 任务：**
- P0-1 文档解析与结构化
- P0-2 多粒度切分与索引
- 设计统一的"结构化文档"数据模型
- 设计向量库 schema 与 BM25 索引结构
- 提供索引查询的底层 SDK（供 B 调用）

**P1 任务：**
- 配合 C 实现 P1-2 文档结构思维导图（提供层级树数据）
- 配合 C 实现 P1-4 文档热力图（提供段落坐标 + 相关度接口）

**P2 任务：**
- P2-2 推荐问题与文档导读生成
- P2-5 实验评估系统的数据准备部分

**对外提供的接口：**
- `parse_document(file)` → 结构化文档对象
- `build_index(doc)` → 索引完成
- `query_index(query, level, top_k)` → 候选片段列表
- `get_doc_tree(doc_id)` → 层级树 JSON

---

#### 👤 成员 B：检索 & 生成线（智能侧）

**P0 任务：**
- P0-3 基础检索引擎（dense + sparse + RRF + rerank）
- P0-4 两阶段层级检索
- P0-5 答案生成与引用
- P0-8 baseline 朴素 RAG 实现
- 设计统一的"检索结果"数据模型

**P1 任务：**
- P1-1 流式思考过程的后端 SSE 实现
- P1-3 检索路径数据结构生成（供 C 渲染）
- P1-5 对比模式的双引擎并行调用
- P1-6 查询改写（HyDE + 多查询扩展）

**P2 任务：**
- P2-1 多文档对比问答
- P2-3 引用可信度评分算法
- P2-4 多轮对话上下文管理

**对外提供的接口：**
- `retrieve(query, mode, options)` → 检索结果（含路径信息）
- `generate(query, contexts)` → 流式答案 + 引用
- `ask(query, mode)` → 端到端问答（封装上面两个）

---

#### 👤 成员 C：前端可视化 & 系统集成线（体验侧）

**P0 任务：**
- P0-6 后端 API 网关搭建（FastAPI 主框架 + 路由）
- P0-7 前端基础交互（上传、列表、问答主界面、引用跳转）
- 项目脚手架（前后端工程结构、Docker Compose）
- API Mock 数据（让前端不依赖 A、B 的进度先跑起来）

**P1 任务：**
- P1-1 流式思考过程的前端展示
- P1-2 文档结构思维导图（用 React Flow / 思维导图库）
- P1-3 检索路径可视化（React Flow 动画）
- P1-4 文档热力图（缩略图组件 + 染色）
- P1-5 对比演示模式 UI
- P1-7 性能监控面板

**P2 任务：**
- P2-2 推荐问题与导读的前端展示
- P2-3 可信度评分的可视化
- P2-4 多轮对话 UI

**对外提供的接口：**
- 统一的 API 网关（聚合 A、B 的能力）
- Mock Server（让 A、B 也能独立测试）

---

### 4.3 并行性保障机制

**关键：先约定接口，再各自开工**

#### 第一步（团队共同，0.5 天）
三人一起开会，确定：
1. **数据模型**（结构化文档、检索结果、引用、检索路径）→ 写成 TypeScript / Pydantic 类型定义
2. **API 契约**（每个接口的 URL、请求体、响应体）→ 写成 OpenAPI / Markdown
3. **Mock 数据**（每个接口的示例返回）→ 由 C 准备

#### 第二步（三人完全并行）
- A 在自己分支开发解析与索引，用 mock query 测试
- B 在自己分支开发检索与生成，用 mock document 测试
- C 在自己分支开发前端，调用 mock API 测试

#### 第三步（联调）
当 A、B 完成 P0 后，依次替换 mock 为真实实现。

### 4.4 并行性具体保障

| 潜在阻塞 | 解决方案 |
|---------|---------|
| B 需要 A 的索引才能做检索 | A 提前提供"假索引"（一份固定数据），B 基于此开发 |
| C 需要 B 的接口才能做前端 | C 自己写 Mock Server 返回假数据 |
| C 需要 A 的文档树才能做思维导图 | A 提前提供一份示例文档树 JSON |
| 三人都要改主程序入口 | C 负责主框架，A、B 以模块形式注入 |

### 4.5 共同负责事项

- 系统集成与联调
- 实验评估（P2-5）
- 期末报告撰写
- 答辩 demo 打磨

---

## 五、关键交付物

### 5.1 代码交付
- 完整可运行的系统（一键 docker-compose up）
- 前后端代码 + README
- API 文档（自动生成）

### 5.2 文档交付
- 期末报告（含系统架构、方法设计、实验分析）
- 演示视频
- 答辩 PPT

### 5.3 演示交付
- 在线可访问的 demo（或本地一键启动）
- 准备 3-5 个典型 demo case（不同长度、不同类型的文档）

---

## 六、风险与应对

| 风险 | 应对策略 |
|------|---------|
| LLM API 成本超支 | 优先用 DeepSeek（便宜），限制单次 token；准备本地小模型兜底 |
| 长文档解析效果差 | 提前调研 unstructured / MinerU 等工具，准备多套方案 |
| 前端可视化超出能力 | 优先用现成库（React Flow、ECharts），不自己造轮子 |
| 评估数据集难找 | 准备自建小规模数据集（10-20 个 query 即可，配合 LLM-as-judge） |
| 联调阶段出问题 | 严格遵守接口契约；每周一次小集成 |

---

## 七、下一步行动

1. **三人共同**：开 kickoff 会，确认本文档，敲定接口契约
2. **成员 C**：搭建项目脚手架 + Mock Server，1-2 天内交付
3. **成员 A、B**：基于 Mock 开始各自 P0 任务
4. **每周**：一次同步会，对齐进度与接口变更

